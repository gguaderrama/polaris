const initialState = {
    active: '/'
}

const reservaclase = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state
    }
}

export default lugareswork
