const initialState = {
    active: '/tienda',
    alert: false
}

const reservaclase = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state
    }
}

export default reservaclase
